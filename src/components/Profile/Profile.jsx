import style from './Profile.styled';

const Profile = () => {
 
  return (
    <section className={style.MenuContainer}>
      <h1 className={style.UserName}>

      </h1>
    </section>
  );
};

export default Profile;